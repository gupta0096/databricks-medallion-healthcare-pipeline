# databricks-medallion-healthcare-pipeline
This project demonstrates a production-style ETL pipeline built on Azure Databricks using the Medallion Architecture (Bronze, Silver, Gold).  The pipeline automatically processes hospital data as new files arrive and produces analytics-ready fact and dimension tables.
